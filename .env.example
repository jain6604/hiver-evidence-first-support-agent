# Create a free key at https://aistudio.google.com/app/apikey, then paste it here.
GEMINI_API_KEY=
# Optional: change only if a model is unavailable for your free-tier account.
GEMINI_MODEL=gemini-3.6-flash
